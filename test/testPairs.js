let pairs = require('../pairs');

const testObject = {
  name: 'Bruce Wayne',
  age: 36,
  location: 'Gotham',
}; // use this object to test your functions

const result = pairs(testObject);

console.log(result);
