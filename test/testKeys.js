let keys = require('../keys');

const testObject = {
  name: 'Bruce Wayne',
  age: 36,
  location: 'Gotham',
}; // use this object to test your functions

let result = keys(testObject);

console.log(result);
