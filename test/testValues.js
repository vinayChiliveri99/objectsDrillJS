let values = require('../values');

const testObject = {
  name: 'Bruce Wayne',
  age: 36,
  location: 'Gotham',
}; // use this object to test your functions

const result = values(testObject);

console.log(result);
